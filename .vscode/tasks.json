{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "react start",
            "type": "shell",
            "command": "yarn start",
            "group": "build",
            "options": {
              "cwd": "${workspaceFolder}"
            },
        }
    ]
}
